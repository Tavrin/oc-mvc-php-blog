{% extends 'layout/admin_index_base.html.twig' %}

{% block head %}
    {{ parent() }}
    {% include 'partials/inc/editor-import.html.twig' %}
{% endblock %}

{% block main %}
<main class="">
    <div class="maw-70 m-auto mt-3 mb-3 d-f fd-c">
        <section id="home-presentation" class="d-f jc-c m-auto pt-3 pb-3 mb-3">
            <div class="ta-l">
                <h1 class="fw-600 fs-3-5 lh-3c5">{{ content.title|default('Nouveau média') }}</h1>
            </div>
        </section>
        <section class="d-f fd-c ai-c">
            <div class="ta-r as-fs pb-2">
                <a href="/admin/structure/medias/{{ content.types }}" class="m-0-5 button-bb-wc br-5 fw-600">< Revenir aux {{ content.type }}s</a>
            </div>
            {{ form.render|raw }}
        </section>
    </div>
</main>
{% endblock %}
